<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".DebugPlaybackReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="lan_audio.debug.START_PLAYBACK" />
                <action android:name="lan_audio.debug.STOP_PLAYBACK" />
                <action android:name="lan_audio.debug.SET_AUDIO_MODE" />
                <action android:name="lan_audio.debug.DUMP_METRICS" />
                <action android:name="lan_audio.debug.OPUS_SELF_TEST" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
